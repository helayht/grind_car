# 轨面代表廓形提取与标准轨面对齐方案

## 1. 目标

本文档用于说明如何从一段轨面的三维点云数据中提取代表廓形，并与标准轨面函数进行对齐和偏差分析。

目标包括：

- 从三维点云中提取多条二维截面
- 生成稳定、可解释的代表廓形
- 将代表廓形与标准轨面函数对齐
- 输出横向偏移、高程偏移、轮廓残差和稳定性指标

## 2. 输入数据与坐标约定

原始三维点云记为：

```text
P = {(x, y, z)}
```

建议采用以下坐标定义：

- `X`：扫描前进方向或里程方向
- `Y`：轨面横向
- `Z`：高度

标准轨面函数记为：

```text
z = f(x), x ∈ [-35.4, 35.4]
```

已知标准函数如下：

```text
y = sqrt(169 - (x + 22.42)^2) + 161.15,   -35.4 <= x < -25.3
y = sqrt(6400 - (x + 7.3)^2) + 95.88,     -25.3 <= x < -10
y = sqrt(90000 - x^2) - 124,              -10 <= x <= 10
y = sqrt(6400 - (x - 7.3)^2) + 95.88,     10 < x <= 25.35
y = sqrt(169 - (x - 22.42)^2) + 161.15,   25.35 < x <= 35.4
```

后续将该标准函数统一记作 `f(x)`。

## 3. 总体流程

推荐采用以下处理流程：

1. 从三维点云中提取多条二维截面
2. 对每条截面做去噪和统一横向采样
3. 将每条截面与标准轨面函数进行对齐
4. 将所有有效截面融合成代表廓形
5. 将代表廓形再次拟合到标准轨面函数
6. 输出偏移量、残差、稳定性指标

## 4. 阶段一：提取多条二维截面

### 4.1 目标

从三维点云中，沿 `X` 方向取一段前进区间，并在该区间内提取用于生成代表廓形的二维截面。每条截面的点表示为一组 `(Y, Z)` 点。

### 4.2 方法

对目标前进位置中心 `x_k`，设置一个窗口半宽 `h_x`，取满足以下条件的点：

```text
|x - x_k| <= h_x
```

这些点构成第 `k` 条截面的原始点集：

```text
S_k = {(y_i, z_i)}
```

### 4.3 参数建议

- 截面步长 `s_x`：`0.5 mm` 到 `2.0 mm`
- 截面窗口半宽 `h_x`：`0.2 mm` 到 `1.0 mm`

初始建议：

- `s_x = 1.0 mm`
- `h_x = 0.5 mm`

## 5. 阶段二：单截面预处理

### 5.1 目标

将每条原始截面转换为统一、稳定、可比较的离散轮廓。

### 5.2 步骤

#### 5.2.1 去除无效点与异常点

建议剔除以下点：

- `X` 超出轨面有效范围较多的点
- 明显飞点或离群点
- 断裂严重或采样过稀的区域点

可先设置横向保留范围：

```text
Y ∈ [-35.4 - margin, 35.4 + margin]
```

其中 `margin` 可取 `2 mm` 到 `5 mm`。

#### 5.2.2 按 Y 分组

不要按完全相同的 `Y` 分组，应按横向网格分箱。

设横向步长为 `d_y`，则可使用：

```text
y_bin = round(y / d_y) * d_y
```

将相同 `y_bin` 的点归为一组。

建议初始取值：

- `d_y = 0.2 mm`

#### 5.2.3 每个 X 分组内做统计

对每个 `y_bin` 内的 `Z` 值，建议优先采用：

- 中位数 `median(Z)`
- 或去极值后的均值

得到规则化截面：

```text
C_k = {(y_j, z_k(y_j))}
```

### 5.3 单截面质量控制

建议剔除以下截面：

- 有效点数量过少
- 左右有效范围过短
- 连续缺失段过多
- 局部波动异常大

## 6. 阶段三：单截面与标准轨面对齐

### 6.1 目标

将每条实测截面对齐到标准轨面函数 `f(y)` 对应的统一参考坐标系中。

### 6.2 对齐模型

对于第 `k` 条截面，设其横向和高度补偿量分别为：

```text
Δy_k, Δz_k
```

对齐后满足：

```text
z_k(y_j) + Δz_k ≈ f(y_j + Δy_k)
```

### 6.3 误差函数

定义最小二乘目标函数：

```text
J_k(Δy_k, Δz_k) = Σ [z_k(y_j) + Δz_k - f(y_j + Δy_k)]^2
```

目标是求使 `J_k` 最小的 `Δy_k` 和 `Δz_k`。

### 6.4 推荐求解方法

推荐只搜索 `Δy_k`，并对 `Δz_k` 采用解析计算。

对于固定 `Δy_k`，最佳 `Δz_k` 可取：

```text
Δz_k = mean(f(y_j + Δy_k) - z_k(y_j))
```

然后在给定范围内搜索 `Δy_k`：

```text
Δy_k ∈ [-2.0 mm, 2.0 mm]
```

建议两阶段搜索：

- 粗搜索步长：`0.05 mm`
- 精搜索步长：`0.01 mm`

### 6.5 对齐后的截面

对齐完成后，第 `k` 条截面可记为：

```text
C_k_aligned = {(y_j + Δy_k, z_k(y_j) + Δz_k)}
```

## 7. 阶段四：生成代表廓形

### 7.1 目标

将所有有效、已对齐的截面融合成一条代表廓形曲线。

### 7.2 方法

在统一横向网格 `y_j` 上，收集所有截面的对应高度值：

```text
Z_j = {z_1(y_j), z_2(y_j), ..., z_m(y_j)}
```

对每个 `y_j` 计算代表值。

### 7.3 统计方式建议

优先建议：

- 中位数 `median(Z_j)`

如需更平滑结果，可考虑：

- 截尾均值
- 去除异常点后的均值

最终代表廓形记为：

```text
R = {(y_j, z_rep(y_j))}
```

### 7.4 同时输出稳定性指标

建议同步输出：

- `std(x_j)`：标准差
- `count(x_j)`：有效样本数

输出形式例如：

```text
(y_j, z_rep(y_j), std(y_j), count(y_j))
```

## 8. 阶段五：代表廓形与标准轨面全局拟合

### 8.1 目标

代表廓形生成后，再整体与标准轨面函数做一次全局拟合，获得最终参考偏移量。

### 8.2 模型

设代表廓形为 `z_rep(y)`，求全局补偿：

```text
Δy_rep, Δz_rep
```

使：

```text
z_rep(y) + Δz_rep ≈ f(y + Δy_rep)
```

### 8.3 误差函数

```text
J_rep(Δy_rep, Δz_rep) = Σ [z_rep(y_j) + Δz_rep - f(y_j + Δy_rep)]^2
```

同样建议采用：

- `Δy_rep` 搜索
- `Δz_rep` 解析求解

## 9. 阶段六：偏差与磨耗分析

### 9.1 轮廓残差

代表廓形对齐到标准轨面后，定义残差：

```text
e(y_j) = z_rep(y_j) + Δz_rep - f(y_j + Δy_rep)
```

### 9.2 可输出指标

建议输出以下指标：

- 横向偏移 `Δy_rep`
- 高程偏移 `Δz_rep`
- 每个横向位置的残差 `e(y_j)`
- 最大正偏差
- 最大负偏差
- 均方根误差 `RMSE`
- 各位置标准差 `std(y_j)`
- 各位置有效样本数 `count(y_j)`

### 9.3 工程意义

这些指标可用于：

- 判断轨面整体相对标准轮廓的偏移情况
- 评估代表廓形与标准轨面的形状误差
- 为磨耗分析提供基础数据

## 10. 推荐默认参数

初始版本可优先采用以下参数：

- `s_x = 1.0 mm`
- `h_x = 0.5 mm`
- `d_y = 0.2 mm`
- `Δy` 搜索范围：`[-2.0, 2.0] mm`
- `Δy` 粗搜索步长：`0.05 mm`
- `Δy` 精搜索步长：`0.01 mm`
- 单截面统计方式：中位数
- 多截面融合方式：中位数

## 11. 时间复杂度

设：

- 原始点云点数为 `N`
- 截面数量为 `M`
- 每条截面的横向采样点数为 `K`
- 单次横向搜索的候选数为 `T`

则：

### 11.1 截面提取与分组

通常接近：

```text
O(N)
```

### 11.2 每条截面与标准函数拟合

复杂度约为：

```text
O(M * T * K)
```

### 11.3 多截面融合

复杂度约为：

```text
O(M * K)
```

## 12. 实施建议

建议按以下顺序实现：

1. 先完成三维点云按 `X` 提取截面的功能
2. 再完成单截面按 `Y` 分组和统计
3. 实现标准轨面函数 `f(x)`
4. 将标准轨面函数中的自变量理解为横向坐标 `Y`，并实现 `f(y)`
5. 完成单截面对标准函数的 `Δy / Δz` 拟合
6. 完成多截面融合生成代表廓形
7. 最后输出残差和评价指标

## 13. 关键注意事项

- 不要直接对整段三维点云做整体平均
- 不要未经对齐就将多条截面直接融合
- 横向网格过细会导致噪声放大
- 截面窗口过宽会降低代表性
- 拟合时不要使用超出标准函数定义域的点
- 建议对坏截面做剔除，避免污染最终结果

## 14. 结论

对于当前轨面代表廓形问题，推荐采用以下主线：

1. 沿 `X` 方向提取多条截面
2. 每条截面按 `Y` 做统一重采样
3. 每条截面分别拟合标准轨面函数
4. 将所有截面对齐到统一参考系
5. 在统一 `Y` 网格上做稳健统计，生成代表廓形
6. 对代表廓形再次拟合标准轨面函数
7. 输出偏移量、残差和稳定性指标

该方案适合轨面廓形检测场景，具有可解释性强、稳定性较好、便于工程实现的特点。
