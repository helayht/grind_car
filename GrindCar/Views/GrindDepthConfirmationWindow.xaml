<Window x:Class="GrindCar.Views.GrindDepthConfirmationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="确认打磨深度"
        Width="760"
        Height="680"
        MinWidth="680"
        MinHeight="560"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource WindowBackgroundBrush}">
    <Window.Resources>

        <Style x:Key="CardBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource CardBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="Padding" Value="18" />
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="{DynamicResource DataGridBackgroundBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource CardBorderBrush}" />
            <Setter Property="GridLinesVisibility" Value="Horizontal" />
            <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource CardBorderBrush}" />
            <Setter Property="RowBackground" Value="{DynamicResource DataGridBackgroundBrush}" />
            <Setter Property="AlternatingRowBackground" Value="{DynamicResource DataGridAltRowBrush}" />
            <Setter Property="HeadersVisibility" Value="Column" />
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{DynamicResource DataGridHeaderBackgroundBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource CardBorderBrush}" />
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource CardBorderBrush}" />
            <Setter Property="Padding" Value="8,4" />
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
            <Setter Property="Background" Value="{DynamicResource WindowBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource CardBorderBrush}" />
            <Setter Property="CaretBrush" Value="{DynamicResource PrimaryTextBrush}" />
            <Setter Property="Padding" Value="6,3" />
        </Style>

    </Window.Resources>

    <Grid Margin="18" Background="{DynamicResource PageBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="1"
                Margin="0,14,0,14"
                Style="{StaticResource CardBorderStyle}">
            <DataGrid AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      HeadersVisibility="Column"
                      ItemsSource="{Binding Items}">
                <DataGrid.Columns>
                    <DataGridTextColumn Width="90"
                                        Header="角度"
                                        IsReadOnly="True"
                                        Binding="{Binding Angle}" />
                    <DataGridTextColumn Width="*"
                                        Header="测量平均打磨深度"
                                        IsReadOnly="True"
                                        Binding="{Binding MeasuredAverageDepth, StringFormat=F6}" />
                    <DataGridTextColumn Width="*"
                                        Header="确认打磨深度"
                                        Binding="{Binding ConfirmedDepthText, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Width="120"
                                        Header="打磨次数"
                                        IsReadOnly="True"
                                        Binding="{Binding GrindingTimes}" />
                    <DataGridTextColumn Width="1.2*"
                                        Header="校验"
                                        IsReadOnly="True"
                                        Binding="{Binding ErrorMessage}" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource CardBorderStyle}">
            <DockPanel LastChildFill="False">
                <TextBlock DockPanel.Dock="Left"
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           Text="{Binding SummaryText}" />
                <StackPanel DockPanel.Dock="Right"
                            Orientation="Horizontal">
                    <Button Width="92"
                            Height="34"
                            Margin="0,0,10,0"
                            Style="{DynamicResource SecondaryButtonStyle}"
                            Content="取消"
                            Click="Cancel_Click" />
                    <Button Width="92"
                            Height="34"
                            Style="{DynamicResource PrimaryButtonStyle}"
                            Content="确认"
                            Click="Confirm_Click" />
                </StackPanel>
            </DockPanel>
        </Border>
    </Grid>
</Window>
